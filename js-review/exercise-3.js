// Buatlah function yang akan mencetak tanggal sekarang ketika dipanggil.
function printCurrentDate() {
    console.log(new Date());
}
printCurrentDate()

// Buatlah function yang memberikan tanggal sekarang ketika dipanggil.
function getCurrentDate() {
    return new Date()
}
console.log(getCurrentDate());